This is the project folder for APS360 class project handwritten math to latex for the progress report. 
The data folder is omitted as it contain ~40G of data. 
Code has two subfolders. data_process and models. Data_process contains the three sets of data (AIDA, hme, im2latex), where AIDA is not used at the end in training. 
and within each dataset folder the data processing code python file is there. 

The models folder contains two subfolders: baseline and transformer, which each contrains the code for constructing the training of the baseline and primary models. 
